package org.shark.mvc;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class MainController {

  /*
   * @RequestMapping
   * 
   * 1. 스프링 MVC에서 웹 요청(HTTP 요청)을 특정 컨트롤러 클래스 또는 특정 컨트롤러 메소드에 매핑할 때 사용하는 어노테이션이다
   * 2. 클라이언트의 요청 주소와 요청 메소드(GET, POST 등)를 컨트롤러의 메소드와 연결하는 역할을 주로 수행한다
   * 
   * 
   * 
   * */
  
  
  
  @RequestMapping(value = "/")
  public String main() {
    return "main";
  }
  
}
